@using OracleDemo.Models

@model UserIndexViewModel
@{
    ViewBag.Title = "テスト";
}
<h2>@ViewBag.Title.</h2>
@using (Html.BeginForm("Execute", "Test", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    <p>非同期テスト</p>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <button type="submit" class="btn btn-default">
                <span>実行</span>
            </button>
        </div>
    </div>
}